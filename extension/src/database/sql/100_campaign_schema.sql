-- Template for creating a campaign schema
-- Replace ${campaign_id} with the sanitized campaign identifier (hyphens replaced with underscores)
-- Example: skua_campaign_my_campaign_name

CREATE SCHEMA IF NOT EXISTS "skua_campaign_${campaign_id}";
