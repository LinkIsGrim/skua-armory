// Tab registry aggregator. Each include adds one entry to the local `_tabs`
// HashMap (declared by the caller — typically fnc_onAdminMenuOpen). Adding
// a new tab = drop a new .inc.sqf next to these and add it here.

#include "tabs\certifications.inc.sqf"
#include "tabs\addonList.inc.sqf"
