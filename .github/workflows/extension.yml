name: Extension

on:
  push:
    branches:
    - master
  pull_request:
    paths:
    - 'extension/**'
    - 'addons/main/script_macros_enums.hpp'
    - '.github/workflows/extension.yml'

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v6

    - name: Setup Rust
      uses: dtolnay/rust-toolchain@stable
      with:
        components: rustfmt

    - name: Cache cargo
      uses: Swatinem/rust-cache@v2

    - name: cargo fmt
      working-directory: extension
      run: cargo fmt --all -- --check

  clippy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v6

    - name: Setup Rust
      uses: dtolnay/rust-toolchain@stable
      with:
        components: clippy

    - name: Cache cargo
      uses: Swatinem/rust-cache@v2

    - name: cargo clippy
      working-directory: extension
      run: cargo clippy --locked --all-targets -- -D warnings -W clippy::pedantic

  test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v6

    - name: Setup Rust
      uses: dtolnay/rust-toolchain@stable

    - name: Cache cargo
      uses: Swatinem/rust-cache@v2

    # ubuntu-latest has Docker pre-installed; testcontainers picks it up.
    - name: cargo test
      working-directory: extension
      run: cargo test --locked
